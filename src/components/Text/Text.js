import React from 'react'
import * as Styled from './styles/StyledText'

export const Text = props => {
  return <Styled.Text {...props} />
}
export const Span = props => {
  return <Styled.Span {...props} />
}
export const ResponsiveText = props => {
  return <Styled.ResponsiveText {...props} />
}
